// Random comment
// Another one!
fn add(x : int, y : int) : int {
  ret x + y;
}

fn sub(x : int, y : int) : int {
  ret x - y;
}

fn test {}

fn main() {
  lem a : int = 5;
  lem b : int = 6, c : int = 10;
  let x : int = 4, y : int = 2;
  let res : int = 0;
  for i in 0 to 10 {
    if true || false {} else {}
    if i < 5 {
      res = call add(x, y);
    } else {
      res = call sub(x, y);
    }
  }
}
